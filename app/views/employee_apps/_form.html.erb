<%= form_with(model: employee_app) do |form| %>
  <% if employee_app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee_app.errors.count, "error") %> prohibited this employee_app from being saved:</h2>

      <ul>
        <% employee_app.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :employee_name %>
    <%= form.text_field :employee_name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="field">
    <%= form.label :gender %>
    <%= form.text_field :gender %>
  </div>

  <div class="field">
    <%= form.label :designation %>
    <%= form.text_field :designation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
